name: Flux_Probe_DeepScan
on:
  repository_dispatch:
    types: [start_smart_render]

jobs:
  deep_scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install_Tools
        run: pip install requests

      - name: Run_Diagnostic_Probe
        run: |
          python - <<EOF
          import requests

          def test_url(desc, url):
              print(f"\n--- TESTING: {desc} ---")
              print(f"URL: {url}")
              
              # Mimic a real Chrome browser to bypass simple bot filters
              headers = {
                  'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36',
                  'Referer': 'https://pollinations.ai/'
              }

              try:
                  r = requests.get(url, headers=headers, timeout=30)
                  print(f"Status Code: {r.status_code}")
                  print(f"File Size: {len(r.content)} bytes")

                  # Logic: If file is small (< 5KB), it is likely an error text, not an image.
                  if len(r.content) < 5000:
                      print("❌ FAILED. SERVER MESSAGE (READ THIS):")
                      print("vvvvvvvvvvvvvvvvvvvvvvvvvvvvvvvv
